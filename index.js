//a function called divide
function divide() {

    return 2000 / 100;
  }
 console.log(divide()); 

//a function called square has a parameter x and uses the arrow function  
  const square = (x) => {
    return x * x;
  }
  console.log(square(2));

  // function add, two parameters, arrow funtion
  const add = (a, b) => {
    return a + b;
  }
  console.log(add(3,4));
  
 
  